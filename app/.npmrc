# Enforce engines field — prevents lockfile drift when running a node/npm
# version that doesn't match the Dockerfile (node:22-alpine → npm 10.9.x).
# Without this, npm only warns on engine mismatches and silently produces a
# lockfile that fails `npm ci` on Railway.
engine-strict=true
